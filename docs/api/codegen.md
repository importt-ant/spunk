# Generator

Generates a fully-typed Python package from either a live `Tenant` object or a
stored manifest dict.  The package exposes one class per service, with typed
attributes for every resource in that service.

::: spunk.generator
