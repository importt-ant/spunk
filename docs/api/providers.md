# Providers

Providers wrap Pulumi provider objects with lazy initialisation so they are
only created when the first resource actually needs them.

::: spunk.providers.provider.Provider

::: spunk.providers.aws.AWSProvider
